# Hackathon Project

## Routing
Routing is powered by the OSRM public API.

We use external routing to keep the repository lightweight
and focus on application logic.

The architecture supports switching to a local OSRM
instance if required in production.
